// const lib = require('lib')({token: process.env.STDLIB_SECRET_TOKEN});

// try {
  // await lib.discord.channels['@0.2.2'].messages.create({
    // channel_id: `913354310430130186`,
    // content: `I joined ${context.params.event.name} - ${context.params.event.member_count} members`,
  // });
// } catch (e) {
  // console.error(e);
  // await lib.discord.channels['@0.2.2'].messages.create({
    // channel_id: `913168010624262154`,
    // content: `I joined ${context.params.event.name} - ${context.params.event.member_count} members`,
  // });
// }

// // instructions on how to setup the bot
// // other stuff?
// // wow such inspiration.